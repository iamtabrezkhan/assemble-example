{
  "name": "assemble-example",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "assemble"
  },
  "license": "ISC",
  "devDependencies": {
    "assemble": "^0.24.3",
    "gulp-extname": "^0.2.2"
  }
}
